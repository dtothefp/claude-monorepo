# Infra Improvement Brief — TEMPLATE

This is the output template the `infra-improver` agent writes against. The agent fills every `<!-- ... -->` block and removes the comment markers in the final brief. Hard rules:

- Each table column caps at **5 items**. Overflow goes to `research/external/_archive/YYYY-MM-DD-infra-overflow.md`.
- `Install Now` column ONLY contains items matching the allowlist in `.claude/context/domains-scope.md`. Default routing for everything else is `Worth Evaluating`.
- Your voice: direct + technical, no em or en dashes as punctuation, no "delve into," no "it's worth noting."
- Per-domain sections are required (all 5), even if a domain has `_No findings this week._`
- The "Proposed TODO additions" section is **proposal-only** through Phase 2. Phase 3 enables allowlist-gated auto-append — that change happens to the agent, not this template.

---

# AI Infrastructure Improvement Brief — YYYY-MM-DD

## TL;DR

<!-- 3 bullets max. The single most important shift across all 5 domains this week + the strongest install candidate + the strongest publish-back opportunity. Each bullet ≤25 words. -->

- **Biggest shift:**
- **Strongest install candidate:**
- **Strongest publish-back opportunity:**

## Items by domain (after filter)

<!-- ASCII bar chart, scale 0-10. Mandatory — render in BOTH chat and disk. -->

```
AI media production    {bar} {N}
Content intelligence   {bar} {N}
Agent orchestration    {bar} {N}
App factory            {bar} {N}
Workspace governance   {bar} {N}

Bar uses ASCII blocks: full block per item, dot for empty slot.
Example for N=8: ████████░░
```

## Cross-domain signal table

<!-- 4 columns × up to 5 rows each. Items can repeat across columns when relevant
     (e.g. an "Install Now" item that's also a "Where You're Ahead" gap-fill). -->

| Install Now | Worth Evaluating | Where You're Ahead | Creators to Track |
|---|---|---|---|
| <!-- name + URL --> | <!-- name + URL --> | <!-- our differentiator + nearest public analog with URL --> | <!-- name + handle/URL + what they shipped this week --> |
| | | | |
| | | | |
| | | | |
| | | | |

## By domain

### 1. AI media production engineering

<!-- 2-5 items, each: bold name, source URL, ≤2 sentence why-it-matters, verdict tag.
     Verdicts: [INSTALL NOW] [EVALUATE] [SKIP — overlap with X] [PUBLISH-BACK].
     Empty section: write "_No findings this week._" -->

### 2. Content intelligence pipelines

<!-- same format -->

### 3. Agent orchestration + multi-model economics

<!-- same format -->

### 4. App factory

<!-- same format -->

### 5. Workspace + wiki governance

<!-- same format -->

## Horizontal trending AI (last30days)

<!-- Pulled from research/external/horizontal-YYYY-MM-DD/<term-slug>-raw-v3-*.md.
     Per term: 1 line summary + top 2-3 items + 1-line angle.
     Cap 12 terms (one block each). If a term returned empty, write "_No signal this week._"
     Format per term block:

     ### {term}
     - **Top items:** <platform> <URL> -- <hook>; <platform> <URL> -- <hook>
     - **Angle:** <one sentence: what's the trend underneath these items>
-->

## Out-of-scope but flagged

<!-- Items that landed in the 2 excluded domains defined in domains-scope.md.
     Bullet pointers, no synthesis. Write "_None this week._" if empty. -->

## Proposed TODO additions

<!-- Specific, copy-pasteable lines the user could add to the relevant TODO.md.
     Format per item:
     - **Project:** packages/<name>/TODO.md
     - **Section:** [this-week | next-week | inbox]
     - **Line:** "- [ ] <imperative action with file paths>"
     - **Why:** <one sentence linking back to a brief item above>

     Phase 1-2: PROPOSAL ONLY. The user copy-pastes accepted items.
     Phase 3: agent allowlist-auto-appends. -->

## Sources scanned this week

- **Newsletters:** <!-- list with hit/miss -->
- **YouTube channels + playlists:** <!-- list with hit count -->
- **Web/forums:** <!-- list with hit count -->
- **GitHub topics:** <!-- list with new-repo count -->
- **Workspace-specific (delegated to existing skills):** newsletter-digest, news-research, youtube-research

## Run metadata

- **Date:** YYYY-MM-DD
- **Brief generated by:** infra-improver agent
- **Domain scope file version:** <!-- git short SHA of .claude/context/domains-scope.md -->
- **Items considered:** <!-- N before filter --> → <!-- M after filter -->
- **Failure modes triggered:** <!-- list any: thin-week, source-down, transcription-failures -->
- **Time:** <!-- HH:MM PT -->
