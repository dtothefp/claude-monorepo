.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
crash.log
tfplan

# Real values carry your org id and work email. The .example is committed.
*.tfvars
!*.tfvars.example

# never commit SA keys
*-sa.json
